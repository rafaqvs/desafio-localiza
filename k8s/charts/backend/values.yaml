replicaCount: 2

image:
  repository: your-backend-image
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

database:
  host: mysql.default.svc.cluster.local
  user: localiza
  password: localizapass
  name: localiza_db

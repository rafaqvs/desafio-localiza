apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: {{ .Values.auth.rootPassword | b64enc }}
  MYSQL_PASSWORD: {{ .Values.auth.password | b64enc }}
